from Applications.Nike import *
from Applications.load_config import *


class TestNike(TestCase):

    """
    def test_login(self):

        driver = WebDriverFactory.get_web_driver_instance(**apps_config['NikeOrder']['webdriver'])
        pl = PageLoginNike(driver=driver, source_id='NikeOrder')
        pl.login(pl.parameters['username'], pl.parameters['password'])
        pl.logout()
        pl.driver.close()
    """